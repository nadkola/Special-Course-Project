# OLAP Assistant — Tier 2 Dependencies
# Install: pip install -r requirements.txt

streamlit>=1.30.0
pandas>=2.0.0
numpy>=1.24.0

# LLM Providers (install at least one)
anthropic>=0.40.0     # For Claude models
openai>=1.10.0        # For GPT models
